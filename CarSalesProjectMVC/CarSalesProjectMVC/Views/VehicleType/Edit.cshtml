@model CarSalesCompanyModels.VehicleType

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>VehicleType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.TypeOfVehicle)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TypeOfVehicle)
            @Html.ValidationMessageFor(model => model.TypeOfVehicle)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
