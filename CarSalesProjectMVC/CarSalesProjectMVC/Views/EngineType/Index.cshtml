@model IEnumerable<CarSalesCompanyModels.EngineType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeOfEngine)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeOfEngine)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeOfEngine }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeOfEngine }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeOfEngine })
        </td>
    </tr>
}

</table>
